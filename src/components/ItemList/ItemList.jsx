const ItemList = ({ products }) => {
    return (
        <section>
            {
                products.map(product => {6
                    return 
                })
            }
        </section>
    )
}

export default ItemList